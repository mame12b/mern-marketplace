import BackButton from '../components/common/BackButton';

const Profile = () => {
    return (
        <div className="container mx-auto px-4 py-8">
            <BackButton />
            <h1 className="text-3xl font-bold text-gray-900 mb-6">Profile Page</h1>
        </div>
    );
};

export default Profile;